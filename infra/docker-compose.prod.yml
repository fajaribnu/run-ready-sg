version: "3.8"

# Production compose — backend only (DB is on RDS)
services:
  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: runready-backend
    env_file:
      - ../backend/.env
    ports:
      - "8000:8000"
    restart: unless-stopped
