[pytest]
testpaths = tests
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (requires RDS)
    e2e: End-to-end tests (requires live server)
    browser: Browser E2E tests using Playwright (requires live site)
